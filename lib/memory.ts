// Memory module — inject context into the system prompt.
// Returns empty string now. Later: load from a DB, file, or vector store.
export async function getMemoryContext(): Promise<string> {
  return '';
}
